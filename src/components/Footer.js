import React from 'react';

const styles = {
  root: {
    minHeight: '4rem',
    height: '2vh',
    width: '100%',
    padding: '1%',
    bottom: '0',
    backgroundColor: 'rgba(50, 50, 50, 1)',
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'row',
    marginBottom: '0',
    marginTop: 'auto',
  },
};

const Footer = () => {
  return (
    <div style={styles.root}>Footer</div>
  );
};

export default Footer;
